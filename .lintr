linters:
  linters_with_defaults(
    defaults = if (requireNamespace("box.linters", quietly = TRUE)) box.linters::rhino_default_linters else lintr::default_linters,
    line_length_linter = line_length_linter(100)
  )
